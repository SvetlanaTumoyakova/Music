@{
    ViewData["Title"] = "Home Page";
}
@model List<Artist>
<div class="container py-5">
    <h1 class="text-center mb-5 display-4">Популярные Артисты</h1>
    <a class="btn btn-primary mb-3" asp-controller="Home" asp-action="Create">Добавить нового исполнителя</a>
    <div class="row">
        @foreach (var artist in Model)
        {
            <div class="col-md-4 col-lg-3">
                <div class="card album-card">
                    <img src="@artist.UrlImg" class="card-img-top" alt="..." />
                    <div class="card-body">
                        <h5 class="card-title">@artist.Name</h5>
                        <a class="btn btn-primary" asp-controller="Home" asp-action="Details" asp-route-id="@artist.Id">Перейти</a>
                    </div>
                    @* <div class="container-fluid">
                        <a class="btn btn-primary" asp-controller="Home" asp-action="Edit" asp-route-id="@artist.Id">Редактировать</a>
                        <a class="btn btn-primary" asp-controller="Home" asp-action="Remove" asp-route-id="@artist.Id">Удалить</a>
                    </div> *@
                </div>
            </div>
        }
    </div>
</div>